{
  "name": "youtube-downloader-api",
  "version": "2.0.0",
  "description": "REST API with dedicated /api/mp3 and /api/mp4 endpoints to download YouTube videos.",
  "type": "module",
  "scripts": {},
  "engines": {
    "node": ">=20.x"
  }
}